## Check Charm

> Returns JSON structured like this:

```json
{
  "isCharmActivated": true,
  "activatedTil": "1562483361"
}
```

Allows to get data about charm (Bean's Blessing) for given ETH Address.

### Endpoint

`GET https://api.axieinfinity.com/v1/battle/battle/check-charm/<ETH_ADDRESS>`

<%= partial "includes/api/sharedParams/addressURLParams.md" %>