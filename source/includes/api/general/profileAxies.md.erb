## Profile Axies

> Returns JSON structured like this:

```json
{
  "totalAxies": 2416,
  "axies": [
    SingleAxieJSON,
    ...
  ]
}
```

Allows to retrieve data on Axies from given profile. Returns up to 12 Axies. Can be filtered with various parameters.

### Endpoint

`GET /addresses/<ETH_ADDRESS>/axies`

<%= partial "includes/api/sharedParams/addressURLParams.md" %>
<%= partial "includes/api/sharedParams/axiesQueryParams.md" %>


